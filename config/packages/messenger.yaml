framework:
    messenger:
        # Uncomment this (and the failed transport below) to send failed messages to this transport for later handling.
        # failure_transport: failed

        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            # failed: 'doctrine://default?queue_name=failed'
            # sync: 'sync://'

        routing:
            # Route your messages to the transports
            App\Messenger\Message\ContactMessageNotification: async

when@test:
    framework:
        messenger:
            transports:
                sync: 'sync://'
            routing:
                App\Messenger\Message\ContactMessageNotification: sync

# @todo Pour les environnements de staging et de production, utiliser Supervisor
# (https://symfony.com/doc/current/messenger.html#supervisor-configuration)
# Pour l'environnement de développement : bin/console messenger:consume async -vv